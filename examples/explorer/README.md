> Created/edited by GitHub Copilot; pending human review.

# Component explorer

The live pages make no Sefaria request when opened. Select **Start live demo** or an example preset to assign the `sref` attribute. They keep transport errors visible, do not replace failures with fixture success, and use public raw-input and acquisition properties, read-only status, scalar presentation attributes, and property bindings for default-true booleans that must be false.
