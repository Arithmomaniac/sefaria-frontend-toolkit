> Created/edited by GitHub Copilot; pending human review.

# React Vite example

The initial Source Card receives validated supplied `Micah 6:8` `data` and makes no request. Submitting the form clears `data` and assigns `sref`. React owns draft input, presentation properties, canonical readouts, and the explicit activation gate; the element owns acquisition, cancellation, stale-result suppression, private preparation, and errors. Stable element identity and StrictMode cleanup are preserved.

Build the workspace before starting the direct development server:

```powershell
pnpm build
pnpm --filter @sefaria-example/react-vite dev
```
